---
import FeatureCard from "./FeatureCard.astro";
---

<section id="features" class="px-6 py-20 lg:py-28">
  <div class="mx-auto max-w-6xl">
    <div class="mb-12 max-w-2xl">
      <p class="mb-3 text-sm font-semibold uppercase tracking-wider text-garden-600 dark:text-garden-400">
        核心功能
      </p>
      <h2 class="text-3xl font-bold tracking-tight text-slate-900 dark:text-white sm:text-4xl">
        不是又一个标签管理器，是一套完整工作流
      </h2>
      <p class="mt-4 text-lg text-slate-600 dark:text-slate-300">
        从聚合、分组、稍后处理到跨设备保存，Tab Garden 把"找回那个标签"从 5 分钟压到 3 秒。
      </p>
    </div>

    <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
      <FeatureCard
        icon="layers"
        title="虚拟分组看板"
        description="按域名自动聚合，或用 9 种颜色自定义分组——所有分组只存在于看板中，绝不调用 tabGroups 权限，浏览器标签栏始终原样。"
        tags={["自动聚合", "9 色", "不碰浏览器"]}
      />
      <FeatureCard
        icon="monitor"
        title="全屏看板 · 多窗口聚合"
        description="跳出 390px 的弹窗拥挤。一张全屏看板聚合所有正常窗口的标签，按窗口来源标记，可按窗口过滤，搜索标题/URL/域名实时过滤。"
        tags={["多窗口", "实时搜索", "窗口过滤"]}
      />
      <FeatureCard
        icon="mouse-pointer"
        title="拖拽与瀑布流布局"
        description="HTML5 拖拽：标签在分组间拖动、整组重新排序。自动补位的瀑布流网格，也可切换手动布局——桌面/平板/手机位置独立保存、互不覆盖。"
        tags={["拖拽", "瀑布流", "按设备记忆"]}
      />
      <FeatureCard
        icon="bookmark"
        title="稍后处理（标签 Snooze）"
        description="把「等会再看」的标签收起并关闭，自定义提醒时间（默认 09:00 / 14:00 / 18:00，最多 5 个）。到点看板高亮提醒，一键重新打开。"
        tags={["关闭即归档", "定时提醒"]}
      />
      <FeatureCard
        icon="briefcase"
        title="工作区 · 跨设备保存"
        description="手动挑选标签命名成工作区，只保存标题与 URL，同步到你自己的 Supabase。换电脑登录即可恢复或编辑，扩展绝不自动上传当前标签页。"
        tags={["手动策展", "只同步标题+URL"]}
      />
      <FeatureCard
        icon="copy-x"
        title="重复标签清理"
        description="一键扫描所有窗口，按精确 URL 聚合重复项，预览「保留/关闭」清单后确认清理。预览后状态变化的标签会自动跳过，避免误关。"
        tags={["全窗口扫描", "预览确认"]}
      />
    </div>
  </div>
</section>
